export enum Permissions {
  ALL = '*',
  MANAGE_ROLES = 'manage_roles',
  MANAGE_EVENTS = 'manage_events',
  MANAGE_REGISTERS = 'manage_registers',
  MANAGE_FUNDRAISINGS = 'manage_fundraisings',
  MANAGE_ANNOUNCEMENTS = 'manage_announcements',
}
