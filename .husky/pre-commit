# Lint et format uniquement les fichiers stagés
echo "🔧 Vérification et formatage des fichiers modifiés..."
npx lint-staged

if [ $? -ne 0 ]; then
  echo "❌ Le linting ou le formatage a échoué. Corrigez les erreurs avant de commiter."
  exit 1
fi

echo "✅ Les vérifications de code sont passées !"